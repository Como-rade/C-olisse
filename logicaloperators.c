#include <stdio.h>

int main (){

    int temp = 0;

    //&& and, || or, ! not
    // <= less or equal, >= greater or equal
    if(temp > 0 && temp < 30){
        printf("The temperature is good\n");

    }else{
        printf("The temperature is bad\n");
    }
}
